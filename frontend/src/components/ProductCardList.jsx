import { useState } from "react"
// import { ProductCard } from ./components/ProductCard

export const ProductCardList = () => {
  const [listItem, setListItem] = useState([])

  //här vet jag inte riktigt, men det borde vara någon form av event?
  //plus det borde vara någon event prevent default?
  const handleClick = (e) => {
    setListItem = handleClick
  }

  return (
    
  )
}